resources:
  - argocd.yaml
  - cert-manager.yaml
  - external-dns.yaml
  - local-path-provisioner.yaml
  - media
  - metallb.yaml
  - namespaces.yaml
  - pihole.yaml
  - storage.yaml
  - traefik.yaml

patches:
  - target:
      kind: Application
    path: patches/argocd-finalizers.yaml
  - target:
      kind: Application
    path: patches/argocd-sync-policy.yaml
